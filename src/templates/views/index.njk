<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Rudy album</title>
		<meta name="description" content="A gallery of Rudy the cat images to demo some of the things you can do with css custom properties">
		<link rel="preconnect" href="https://fonts.gstatic.com">
		<link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;700&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="styles/index.css">
		<script async type="module" src="./scripts/index.js"></script>
	</head>
	<body>

		<main>
			<header>
				<h1>Rudy</h1>
			</header>

			<h2>Gallery</h2>
			<div class="rudy-cards">

			{% for imageMeta in imagesMeta %}
				<button class="rudy-cards__card">
					<h3>{{ imageMeta.alt }}</h3>
					<div class="img-wrapper">
						<img 
							alt="Rudy {{ imageMeta.alt }}"
							data-high-res-img="{{ imageMeta.w1200.url }}"
							srcset="{{ imageMeta.w800.url }} 800w,
											{{ imageMeta.w480.url }} 480w,
											{{ imageMeta.w300.url }} 300w"
							sizes="(min-width: 980px) 800px,
										 (min-width: 600px) 480px,
										 (min-width: 340px) 480px,
										 300px"
							src="{{ imageMeta.w300.url }}"
							loading="lazy">
					</div>
				</button>
			{% endfor %}

			</div>
		</main>
	</body>
</html>